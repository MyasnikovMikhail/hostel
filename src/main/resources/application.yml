spring:
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5433/hostel
    username: root
    driver-class-name: org.postgresql.Driver
    password: 123
  sql:
    init:
      platform: postgres
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect